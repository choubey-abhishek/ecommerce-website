/**
 * @deprecated Moved to `components/layout/navbar.tsx` as part of the
 * Phase 1 architecture pass (layout components now live under
 * `components/layout/`, reserving `components/` root for shared,
 * non-layout UI). Re-exported here only so any stale imports don't
 * break the build; update imports to `@/components/layout/navbar`.
 */
export { Navbar as default, Navbar } from "@/components/layout/navbar";
