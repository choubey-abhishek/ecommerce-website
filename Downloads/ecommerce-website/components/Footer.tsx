/**
 * @deprecated Moved to `components/layout/footer.tsx`. Re-exported here
 * only so any stale imports don't break the build; update imports to
 * `@/components/layout/footer`.
 */
export { Footer as default, Footer } from "@/components/layout/footer";
